import { TranslateModule } from '@ngx-translate/core'; import { Component, OnInit } from '@angular/core'; import { CommonModule } from '@angular/common'; import { ActivatedRoute, Router, RouterModule } from '@angular/router'; import { DealService } from '../../../services/deal.service'; import { NotificationService } from '../../../services/notification.service'; @Component({ selector: 'app-deal-details', standalone: true, imports: [CommonModule, RouterModule, TranslateModule], templateUrl: './deal-details.html', styleUrls: ['./deal-details.css'] }) export class DealDetails implements OnInit { deal: any = null; orders: any[] = []; loading: boolean = true; loadingOrders: boolean = false; constructor(private route: ActivatedRoute, private router: Router, private dealService: DealService, private notification: NotificationService) { } ngOnInit(): void { const stateDeal = history.state.deal; if (stateDeal) { this.deal = stateDeal; this.loading = false; this.fetchOrders(); } else { const id = this.route.snapshot.paramMap.get('id'); if (id) { this.notification.error('Deal data lost on refresh. Navigating back...'); setTimeout(() => { window.history.back(); }, 2000); } } } fetchOrders() { if (!this.deal || !this.deal.id) return; this.loadingOrders = true; this.dealService.getDealOrders(this.deal.id).subscribe({ next: (response) => { if (response.errorcode === "0") { this.orders = response.data.data; } this.loadingOrders = false; }, error: () => { this.loadingOrders = false; } }); } approve() { this.dealService.approveDeal(this.deal.id).subscribe({ next: () => { this.notification.success('Deal approved successfully'); this.deal.status = 'approved'; setTimeout(() => this.router.navigate(['/deals']), 1500); } }); } reject() { const reason = prompt('Please enter the reason for rejection:'); if (reason && reason.trim()) { this.dealService.rejectDeal(this.deal.id, reason).subscribe({ next: () => { this.notification.success('Deal rejected successfully'); this.deal.status = 'rejected'; setTimeout(() => this.router.navigate(['/deals']), 1500); } }); } } }